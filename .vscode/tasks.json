{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    // started with this, then swapped to live server for the hot reload
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "explorer.exe",
            "args": ["resume.html"]
        }
    ]
}
